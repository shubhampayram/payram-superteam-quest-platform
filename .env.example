# Admin credentials — REQUIRED before running
# Change ADMIN_PASSWORD to a strong secret before deploying
ADMIN_EMAIL=admin@payram.co
ADMIN_PASSWORD=change-me-to-a-strong-password

# Resend email service (optional — emails log to console if missing)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# App public URL (used in email links)
APP_URL=http://localhost:3000
